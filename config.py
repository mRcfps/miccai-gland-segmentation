# Sampled patch size (also the input and output size of model)
PATCH_SIZE = 384

# Number of iterations for each epoch
# This is roughly equal to Total area of training images divided by patch area
PATCHES_PER_EPOCH = 300
